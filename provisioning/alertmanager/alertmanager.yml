global:
  resolve_timeout: 5m  # Wait 5 minutes before resolving an alert

route:
  receiver: "email"
  repeat_interval: 20s  # Alert every 20 seconds if still active
  group_by: [alertname]

receivers:
  - name: "email"
    email_configs:
      - to: "transcendence.42institute@gmail.com"  # Change to your desired recipient email
        from: "transcendence.42institute@gmail.com"  # From email address
        smarthost: "smtp.gmail.com:587"  # Gmail SMTP server
        auth_username: "transcendence.42institute@gmail.com"  # Your email address for authentication
        auth_password: "Tr@nscendence@42"  # Your email password (preferably use environment variables for this)
        auth_identity: "transcendence.42institute@gmail.com"  # Identity for SMTP authentication
        tls_config:
          insecure_skip_verify: false  # Set to true if you want to skip TLS verification
        headers:
          subject: "🚨 Prometheus Alert"